#Read a first name and last name from input, then print "Hello firstname lastname! You just delved into python." with the given names inserted.

def print_full_name(first, last):
    print(f"Hello {first} {last}! You just delved into python.")

if __name__ == '__main__':
    first_name = input()
    last_name = input()
    print_full_name(first_name, last_name)
